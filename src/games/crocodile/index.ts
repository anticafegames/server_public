import { iContainerGame } from '../common/games-container'
import GameEntity from './entity'
import addData from './add-data'

const container: iContainerGame = { gameKey: 'crocodile', gameEntity: GameEntity, addData }

export default container 