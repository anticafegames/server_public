import { iContainerGame } from '../common/games-container'
import GameEntity from './entity'

const container: iContainerGame = { gameKey: 'crocodile', gameEntity: GameEntity }

export default container 